import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.csiab',
  appName: 'cs-iab',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
